import { Title } from "@/components/shared";

interface Props {
  className?: string;
}

const Checkout: React.FC<Props> = ({ className }) => {
  return <div>
    <Title text="jjjgjgjgj"/>
  </div>;
};

export default Checkout;
